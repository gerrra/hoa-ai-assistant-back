# --- Database ---
DB_HOST=db
DB_PORT=5432
DB_NAME=hoa
DB_USER=hoa
DB_PASS=change_me
DB_URL=postgresql+psycopg://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}
DB_DSN=postgresql://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# --- App ---
OPENAI_API_KEY=
SECRET_KEY=
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_me
CORS_ORIGINS=["https://app.gerrra.com","https://admin.gerrra.com"]
